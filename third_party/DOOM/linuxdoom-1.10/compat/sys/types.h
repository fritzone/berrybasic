#ifndef _COMPAT_SYS_TYPES_H
#define _COMPAT_SYS_TYPES_H
/* Minimal <sys/types.h> for the BerryBasiC DOOM port. DOOM defines its own
 * fixed-width types in doomtype.h; this just satisfies the include. */
#include <stddef.h>
#endif
